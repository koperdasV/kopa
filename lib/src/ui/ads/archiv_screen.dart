import 'package:flutter/material.dart';

class ArchivScreen extends StatelessWidget {
  const ArchivScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('archiv'));
  }
}