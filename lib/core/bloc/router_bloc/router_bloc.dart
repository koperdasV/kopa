// import 'package:flutter_bloc/flutter_bloc.dart';
// import 'package:kopa/core/bloc/navigation_bloc/navigation_event.dart';
// import 'package:kopa/core/bloc/navigation_bloc/navigation_state.dart';

// class NavigationBloc extends Bloc<NavigationEvent, RouterState> {
//   NavigationBloc() : super(HomeState()) {
//     on<HomeEvent>((event, emit) => emit(HomeState()));
//     on<AdsEvent>((event, emit) => emit(AdsState()));
//     on<FavoriteEvent>((event, emit) => emit(FavoriteState()));
//     on<ProfileEvent>((event, emit) => emit(FavoriteState()));
//     on<DetailsEvent>((event, emit) => emit(DetailsState()));
//   }
// }
